# claude-progress.md — memoria de sesiones del harness de __PROJECT_NAME__

Log narrativo sesión por sesión (Session 001, Session 002, …), append-only:
nunca se reescribe una sección vieja, solo se agrega una nueva al final.
Cada sesión (implementer o verifier) deja su propia sección con: goal, qué
se hizo, verificación que corrió (comando + salida esencial — no solo el
puntero a un log efímero), commits, riesgos, next best step.

Generado vacío por `bootstrap.sh` el __TODAY__ — la primera sesión real
agrega la Session 001 acá abajo.
